/**
 * An interface that determines that whether an object can have a portal placed on it.
 */
 
public interface Portalable
{
  /**
   * Returns whether or not the wall can Portal.
   */
  public boolean canPortal(); 
}